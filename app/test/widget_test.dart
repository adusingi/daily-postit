import 'package:flutter_test/flutter_test.dart';
import 'package:daily_postit/main.dart';

void main() {
  test('DailyPostItApp can be constructed', () {
    const app = DailyPostItApp();
    expect(app, isNotNull);
  });
}
